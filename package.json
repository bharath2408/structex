{
  "name": "structex-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/structex && npm run build -w packages/create-structex",
    "typecheck": "npm run typecheck -w packages/structex && npm run typecheck -w packages/create-structex",
    "test": "npm run test -w packages/structex && npm run test -w packages/create-structex",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "allowScripts": {
    "esbuild@0.27.7": true
  }
}
